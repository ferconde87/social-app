<% provide(:title, 'Friends') %>
<h1 class="text-center my-5 text-primary">Friends</h1>

<div class="container ">
  <div class="digg-pagination mb-5 h4">
    <%= will_paginate %>
  </div>
  <ul class="list-group mb-5">
    <%= render @users %>
  </ul>
  <div class="digg-pagination mb-5 h4">
    <%= will_paginate %>
  </div>
</div>
